prefix=@prefix@
exec_prefix=${prefix}
libdir=${exec_prefix}/lib
includedir=${prefix}/include

Name: CTPL
Description: C Template (Parser) Library
Version: @VERSION@
Requires: glib-2.0 libmb
# m is not private since mathutils.h uses it and it is distributed (for now?)
Libs: -L${libdir} -lctpl -lm
Libs.private: 
Cflags: -I${includedir}
